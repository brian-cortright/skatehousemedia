---
import Base from "@/layouts/Base.astro";
import ShuffleIsland from "@/components/islands/ShuffleIsland";
---

<Base
  title="Shuffle — SkateHouseMedia"
  description="Shuffle through random SkateHouseMedia videos — sit back, relax, and let the clips play."
>
  <ShuffleIsland client:only="react" pathname={Astro.url.pathname} />
</Base>
